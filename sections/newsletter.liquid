{%- assign s = section.settings -%}
<section class="section wrap" id="s-{{ section.id }}">
  <div class="newsletter glass" data-specular data-reveal>
    <div>
      <p class="eyebrow">{% render 'tr', value: s.eyebrow, key: 'home.newsletter_eyebrow' %}</p>
      <h2>{% render 'tr', value: s.heading, key: 'home.newsletter_heading' %}</h2>
      <p class="lede" style="margin-top:10px">{% render 'tr', value: s.body, key: 'home.newsletter_body' %}</p>
    </div>
    {%- form 'customer', id: 'newsletter-section', class: 'newsletter__form glass glass--pill' -%}
      {%- if form.posted_successfully? -%}
        <p class="form-success" style="padding:12px 18px">{{ 'newsletter.success' | t }}</p>
      {%- else -%}
        {%- capture pl -%}{% render 'tr', value: s.placeholder, key: 'newsletter.placeholder' %}{%- endcapture -%}
        {%- capture btn -%}{% render 'tr', value: s.button_label, key: 'newsletter.button' %}{%- endcapture -%}
        <input class="input" type="email" name="contact[email]" placeholder="{{ pl | strip }}" autocomplete="email" required aria-label="{{ pl | strip }}">
        <button class="btn btn--primary" type="submit">{{ btn | strip }}{% render 'icon', name: 'arrow-right' %}</button>
      {%- endif -%}
    {%- endform -%}
  </div>
</section>
{% schema %}
{
  "name": "Newsletter",
  "settings": [
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow (blank = translated)"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading (blank = translated)"
    },
    {
      "type": "textarea",
      "id": "body",
      "label": "Body text (blank = translated)"
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "Button label (blank = translated)"
    },
    {
      "type": "text",
      "id": "placeholder",
      "label": "Input placeholder (blank = translated)"
    }
  ],
  "presets": [
    {
      "name": "Newsletter"
    }
  ]
}
{% endschema %}

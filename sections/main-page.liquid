<section class="page wrap">
  <header class="page__head" data-reveal>
    <p class="eyebrow">{{ shop.name }}</p>
    <h1>{% render 'menu-title', title: page.title %}</h1>
  </header>
  <div class="page__body glass" data-reveal><div class="rte">{% render 'localized-content', content: page.content %}</div></div>
</section>
{% schema %}
{ "name": "Page", "settings": [] }
{% endschema %}
